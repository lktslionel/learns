
export function rgbToHex(r: number, g: number,b: number): string {
  let redHex: string
  let greenHex: string
  let bleuHex: string

  return `#{ redHex + bleuHex}`
}


export function hexToRgb(hex: string): { red: number, green: number, bleu: number} {
  return {
    red: 1,
    green: 1,
    bleu: 1,
  }
}
